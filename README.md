# Advanced Valorant Rank Prediction
-  Created by Jessica "Cas" Nappi

## Getting Started
run the following command to install python package requirements.

``pip install -r requirments.txt``

run the following command to open Streamlit UI. 

``streamlit run streamlit_run.py``

Upload a clip or use a virtual webcam to capture a live screen and press start!